- name: Bootstrap
  hosts: truenas

  roles:
    - ssh_server

  # tasks:
    # This does not seem to work.
    # - name: Set Root Password
    #   ansible.builtin.user:
    #     name: root
    #     password: "{{ vault.truenas_root_password | password_hash('sha512') }}"
    #     state: present
